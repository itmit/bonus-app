<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="bonus.app.Views.NewsCell">
    <ContentPage.Content>
        <StackLayout>
            <Grid Margin="25,10">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Frame Grid.Column="0" CornerRadius="10" IsClippedToBounds="True" Padding="0" BackgroundColor="Transparent" Margin="0,0,0,20" BorderColor="Transparent">
                    <Image Source="{Binding Imgsource}" HeightRequest="80" Aspect="AspectFill"/>
                </Frame>
                <Label Grid.Column="1" Text="{Binding Name}" TextColor="#505050" FontSize="16"/>
                <Label Grid.Column="1" Text="{Binding Date}" TextColor="#858585" FontSize="16"/>
            </Grid>
            <Label Text="{Binding Text}" TextColor="#505050" FontSize="16"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="bonus.app.Core.Views.ContentViews.Services.ServiceContentView">
    <ContentView.Content>
        <StackLayout BackgroundColor="{Binding Color}" Spacing="0">
            <StackLayout.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding SelectServiceCommand}"/>
            </StackLayout.GestureRecognizers>
            <Label Text="{Binding Name}" Margin="50,20,5,15" HorizontalOptions="FillAndExpand"/>
            <BoxView Margin="35,0,0,0"/>
        </StackLayout>
    </ContentView.Content>
</ContentView>